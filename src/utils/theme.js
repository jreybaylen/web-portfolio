const spacing = 30
const theme = {
    spacing,
    container: {
        width: '100%',
        maxWidth: 1200,
        margin: 'auto',
        padding: `0 ${ spacing }px`
    },
    transition: '0.3s'
}

export { theme }